import 'package:flutter/material.dart';

class SizeScheme {
  SizeScheme({
    this.height,
    this.iconSize,
    this.labelTextStyle,
  });

  double? height;
  double? iconSize;
  TextStyle? labelTextStyle;
}
